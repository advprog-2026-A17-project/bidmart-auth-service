spring.application.name=bidmartauthservice
server.port=${PORT:8080}
spring.profiles.default=local
<<<<<<< HEAD
=======
spring.config.import=optional:file:.env[.properties],optional:file:../bidmart-infrastructure/.env[.properties]
>>>>>>> 8eafe528816cf21eb420620d439fc17c3edb1a0e

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/bidmart_auth_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

app.auth.email-verification.base-url=${AUTH_EMAIL_VERIFICATION_BASE_URL:http://localhost:3000/verify-email}
app.auth.email-verification.from=${AUTH_EMAIL_VERIFICATION_FROM:no-reply@bidmart.local}
app.auth.email-verification.token-ttl-seconds=${AUTH_EMAIL_VERIFICATION_TOKEN_TTL_SECONDS:86400}
app.auth.email-verification.resend-cooldown-seconds=${AUTH_EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS:60}
oauth.google.client-id=${OAUTH_GOOGLE_CLIENT_ID:}
<<<<<<< HEAD
=======

app.auth.wallet-provisioning.source=${AUTH_WALLET_PROVISIONING_SOURCE:bidmart-auth-service}
app.auth.wallet-provisioning.exchange=${AUTH_WALLET_PROVISIONING_EXCHANGE:bidmart.wallet.provisioning}
app.auth.wallet-provisioning.routing-key=${AUTH_WALLET_PROVISIONING_ROUTING_KEY:wallet.provision.requested.v1}
app.auth.wallet-provisioning.outbox.publish-delay-ms=${AUTH_WALLET_PROVISIONING_OUTBOX_PUBLISH_DELAY_MS:5000}
app.auth.wallet-provisioning.outbox.batch-size=${AUTH_WALLET_PROVISIONING_OUTBOX_BATCH_SIZE:50}
app.auth.wallet-provisioning.outbox.max-attempts=${AUTH_WALLET_PROVISIONING_OUTBOX_MAX_ATTEMPTS:10}
app.auth.wallet-provisioning.outbox.retry-base-seconds=${AUTH_WALLET_PROVISIONING_OUTBOX_RETRY_BASE_SECONDS:30}
app.auth.wallet-provisioning.outbox.retry-max-seconds=${AUTH_WALLET_PROVISIONING_OUTBOX_RETRY_MAX_SECONDS:900}

app.auth.jwt.secret=${AUTH_JWT_SECRET}
app.auth.jwt.access-ttl-seconds=${AUTH_JWT_ACCESS_TTL_SECONDS:900}
app.auth.jwt.refresh-ttl-seconds=${AUTH_JWT_REFRESH_TTL_SECONDS:604800}
app.auth.sessions.max-concurrent=${AUTH_MAX_CONCURRENT_SESSIONS:5}
app.auth.rate-limit.max-attempts=${AUTH_RATE_LIMIT_MAX_ATTEMPTS:5}
app.auth.rate-limit.window-seconds=${AUTH_RATE_LIMIT_WINDOW_SECONDS:60}

# Redis Configuration for Session Caching
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=${REDIS_TIMEOUT:2000}
spring.data.redis.jedis.pool.max-active=${REDIS_POOL_MAX_ACTIVE:8}
spring.data.redis.jedis.pool.max-idle=${REDIS_POOL_MAX_IDLE:8}
spring.data.redis.jedis.pool.min-idle=${REDIS_POOL_MIN_IDLE:0}
>>>>>>> 8eafe528816cf21eb420620d439fc17c3edb1a0e
